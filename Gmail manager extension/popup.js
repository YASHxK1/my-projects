// Select and Delete Emails
document.getElementById("selectDeleteButton").addEventListener("click", () => {
    // Code to select and delete emails
  });
  
  // Sort by Category
  document.getElementById("sortCategoryButton").addEventListener("click", () => {
    // Code to sort emails by category
  });
  
  // Sort by Date
  document.getElementById("sortDateButton").addEventListener("click", () => {
    // Code to sort emails by date
  });
  
  // Sort by Sender
  document.getElementById("sortSenderButton").addEventListener("click", () => {
    // Code to sort emails by sender
  });
  
  // Show Emails from Same Sender
  document.getElementById("showSameSenderButton").addEventListener("click", () => {
    // Code to show emails from the same sender
  });
  